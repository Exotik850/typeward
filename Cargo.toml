[package]
name = "typeward"
version = "0.2.0"
edition = "2024"

[dependencies]
array-util = { version = "1.0", optional = true }
pastey = "0.2.1"

[features]
default = ["arrays"]
arrays = ["dep:array-util"]
